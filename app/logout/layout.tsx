
export const metadata = {
  title: "Tancar sessió",
  robots: {
    index: false,
    follow: false,
  }
};

export default async function LogoutLayout({ children }: { children: React.ReactNode }) {

  return (
      children
  );
}