
export const metadata = {
  title: "Error",
  robots: {
    index: false,
    follow: false,
  }
};

export default async function ErrorLayout({ children }: { children: React.ReactNode }) {

  return (
      children
  );
}